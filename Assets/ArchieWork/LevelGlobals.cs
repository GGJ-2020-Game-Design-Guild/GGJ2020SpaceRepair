using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class LevelGlobal
{
    public static string LevelText;
    public static int broken = 0;

    // Start is called before the first frame update
    
}
